// import { selectJarvisCustomerV2Domain } from '../selectors';

describe('selectJarvisCustomerV2Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
