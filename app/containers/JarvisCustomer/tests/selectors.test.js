// import { selectJarvisCustomerDomain } from '../selectors';

describe('selectJarvisCustomerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
